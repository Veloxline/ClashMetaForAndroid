name: 2. 完美排序 (数学排序版)

on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  mirror-math-sort:
    runs-on: ubuntu-latest
    steps:
      - name: 初始化
        uses: actions/checkout@v4

      - name: 执行排序搬运
        env:
          GH_TOKEN: ${{ secrets.MY_PAT }}
          SOURCE_REPO: MetaCubeX/ClashMetaForAndroid
        run: |
          mkdir -p download_temp
          
          echo "🔍 获取列表并进行【版本号排序】..."
          
          # 【核心修改】
          # 1. 获取 Tag 列表
          # 2. 使用 sort -V (版本排序) 强制从小到大排列 (v2.9 -> v2.10 -> v2.11)
          # 这样脚本就会先创建旧版，最后创建新版。
          gh release list -R $SOURCE_REPO --limit 100 --json tagName -q '.[].tagName' | sort -V > tags.txt
          
          # --- 调试：打印计划 ---
          echo "📊 搬运顺序预演 (前3个 - 最先创建，沉底):"
          head -n 3 tags.txt
          echo "..."
          echo "📊 搬运顺序预演 (后3个 - 最后创建，置顶):"
          tail -n 3 tags.txt
          echo "----------------------------------------"
          
          TOTAL=$(wc -l < tags.txt)
          CURRENT=0
          
          while read TAG; do
            CURRENT=$((CURRENT+1))
            NEW_TAG="CMFA-$TAG"
            
            # 查重
            if gh release view $NEW_TAG > /dev/null 2>&1; then
               echo "[$CURRENT/$TOTAL] ✅ $NEW_TAG 已存在，跳过。"
               continue
            fi

            echo "🚀 [$CURRENT/$TOTAL] 正在搬运: $TAG"
            
            # 1. 抓取信息
            if ! RAW_DATE=$(gh release view $TAG -R $SOURCE_REPO --json publishedAt -q .publishedAt 2>/dev/null); then
               echo "⚠️ 信息读取失败，跳过..."
               continue
            fi
            NICE_DATE=$(date -d "$RAW_DATE" +'%Y-%m-%d %H:%M')
            gh release view $TAG -R $SOURCE_REPO --json body -q .body > logs.txt
            
            echo -e "### 📅 原版发布时间: **$NICE_DATE**\n\n---" > notes.md
            cat logs.txt >> notes.md
            
            # 2. 下载
            rm -rf download_temp/*
            gh release download $TAG -R $SOURCE_REPO -D download_temp 2>/dev/null || true
            
            # 3. 上传
            if [ "$(ls -A download_temp)" ]; then
               gh release create $NEW_TAG download_temp/* --title "CMFA: $TAG" -F notes.md
            else
               gh release create $NEW_TAG --title "CMFA: $TAG (无附件)" -F notes.md
            fi
            
            echo "✅ 完成 $TAG"
            
            # 强制睡眠 2 秒，确保时间戳不重复
            sleep 2
            
          done < tags.txt
          
          rm -rf download_temp tags.txt logs.txt notes.md
          echo "🏁 搬运完成！这次顺序绝对是数学级准确的！"
