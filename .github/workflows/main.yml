name: 2. 纯净搬运 (Python数学排序版)

on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  python-mirror:
    runs-on: ubuntu-latest
    steps:
      - name: 初始化
        uses: actions/checkout@v4

      - name: 执行搬运
        env:
          GH_TOKEN: ${{ secrets.MY_PAT }}
          SOURCE_REPO: MetaCubeX/ClashMetaForAndroid
        run: |
          mkdir -p download_temp
          
          echo "🔍 获取列表..."
          # 1. 抓取所有 v 开头的标签
          gh release list -R $SOURCE_REPO --limit 100 --json tagName -q '.[].tagName' | grep "^v" > raw.txt
          
          echo "🧮 正在进行 Python 严格排序 (从小到大)..."
          
          # --- Python 脚本：保证 v2.9 在 v2.11 前面 ---
          python3 -c "
          import sys
          lines = [l.strip() for l in open('raw.txt') if l.strip()]
          
          # 排序函数：v2.11.9 -> [2, 11, 9]
          def sort_key(s):
              nums = s.lstrip('v').split('-')[0].split('.')
              return [int(x) for x in nums if x.isdigit()]
          
          # 从小到大排序 (Old -> New)
          lines.sort(key=sort_key)
          
          print('\n'.join(lines))
          " > tags.txt
          
          # --- 🚨 调试：打印顺序给您确认 ---
          echo "========================================"
          echo "📊 搬运顺序预演 (如果 v2.9 在上面，v2.11 在下面，就是对的):"
          echo "➡️ 第一个搬 (最旧): $(head -n 1 tags.txt)"
          echo "..."
          echo "➡️ 最后一个搬 (最新): $(tail -n 1 tags.txt)"
          echo "========================================"
          
          if [ ! -s tags.txt ]; then
             echo "❌ 列表为空！"
             exit 1
          fi

          # 锁定最后一行作为 Latest
          LATEST_TARGET=$(tail -n 1 tags.txt)
          TOTAL=$(wc -l < tags.txt)
          CURRENT=0
          
          while read TAG; do
            CURRENT=$((CURRENT+1))
            NEW_TAG="CMFA-$TAG"
            
            # --- 查重：已存在则跳过 (不删!) ---
            if gh release view $NEW_TAG > /dev/null 2>&1; then
              echo "[$CURRENT/$TOTAL] ✅ $NEW_TAG 已存在，跳过。"
              continue
            fi
            
            echo "🚀 [$CURRENT/$TOTAL] 正在搬运: $TAG"
            
            # 1. 准备日志
            gh release view $TAG -R $SOURCE_REPO --json publishedAt,body > meta.json
            RAW_DATE=$(jq -r .publishedAt meta.json)
            NICE_DATE=$(date -d "$RAW_DATE" +'%Y-%m-%d %H:%M')
            
            echo -e "### 📅 原版发布时间: **$NICE_DATE**\n\n---" > notes.md
            jq -r .body meta.json >> notes.md
            
            # 2. 下载
            rm -rf download_temp/*
            gh release download $TAG -R $SOURCE_REPO -D download_temp 2>/dev/null || true
            
            # 3. 上传 (先都设为 false，依靠创建时间排序)
            if [ "$(ls -A download_temp)" ]; then
               gh release create $NEW_TAG download_temp/* --title "CMFA: $TAG" -F notes.md --latest=false
            else
               gh release create $NEW_TAG --title "CMFA: $TAG (无附件)" -F notes.md --latest=false
            fi
            
            echo "✅ $NEW_TAG 完成"
            
            # 🟢 关键：睡眠 3 秒，确保时间戳拉开差距
            sleep 3
            
          done < tags.txt
          
          # --- 终极修正：把皇冠给最后搬运的那个 ---
          if [ ! -z "$LATEST_TARGET" ]; then
             FINAL_TAG="CMFA-$LATEST_TARGET"
             echo "👑 强制设定 Latest: $FINAL_TAG"
             gh release edit "$FINAL_TAG" --make-latest || true
          fi
          
          rm -rf download_temp tags.txt raw.txt meta.json notes.md
          echo "🏁 搬运完成！"
