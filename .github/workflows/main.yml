name: 2. 纯净搬运 (参数修复版)

on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  syntax-fix-sync:
    runs-on: ubuntu-latest
    steps:
      - name: 初始化
        uses: actions/checkout@v4

      - name: 1. 计算与拆分 (Python 排序)
        env:
          GH_TOKEN: ${{ secrets.MY_PAT }}
          SOURCE_REPO: MetaCubeX/ClashMetaForAndroid
        run: |
          mkdir -p download_temp
          
          echo "🔍 获取原始列表..."
          gh release list -R $SOURCE_REPO --limit 100 --json tagName -q '.[].tagName' | grep "^v" > raw.txt
          
          echo "🧮 Python 严格排序..."
          python3 -c "
          import sys
          lines = [l.strip() for l in open('raw.txt') if l.strip()]
          def sort_key(s):
              nums = s.lstrip('v').split('-')[0].split('.')
              return [int(x) for x in nums if x.isdigit()]
          lines.sort(key=sort_key)
          print('\n'.join(lines))
          " > sorted_tags.txt
          
          # 物理隔离：把“唯一的王”单独拿出来
          tail -n 1 sorted_tags.txt > king.txt
          head -n -1 sorted_tags.txt > commoners.txt
          
          KING=$(cat king.txt)
          echo "========================================"
          echo "👑 锁定的新王: $KING"
          echo "👥 平民数量: $(wc -l < commoners.txt)"
          echo "========================================"

      - name: 2. 处理旧版本 (Commoners)
        env:
          GH_TOKEN: ${{ secrets.MY_PAT }}
          SOURCE_REPO: MetaCubeX/ClashMetaForAndroid
        run: |
          set +e # 允许部分报错继续执行
          
          TOTAL=$(wc -l < commoners.txt)
          CURRENT=0
          
          echo "🚀 开始搬运旧版本 (强制设为非 Latest)..."
          
          while read TAG; do
            CURRENT=$((CURRENT+1))
            NEW_TAG="CMFA-$TAG"
            
            # 简单查重
            if gh release view $NEW_TAG >/dev/null 2>&1; then
               echo "⏭️ $NEW_TAG 已存在"
               continue
            fi
            
            echo "Processing [$CURRENT/$TOTAL]: $TAG"
            
            # 抓取信息
            gh release view $TAG -R $SOURCE_REPO --json publishedAt,body > meta.json
            # 如果抓取失败，跳过
            if [ $? -ne 0 ]; then continue; fi
            
            ORIG_DATE=$(jq -r .publishedAt meta.json)
            NICE_DATE=$(date -d "$ORIG_DATE" +'%Y-%m-%d %H:%M')
            echo -e "### 📅 原版发布时间: **$NICE_DATE**\n\n---" > notes.md
            jq -r .body meta.json >> notes.md
            
            # 下载
            rm -rf download_temp/*
            gh release download $TAG -R $SOURCE_REPO -D download_temp 2>/dev/null
            
            # 📤 上传 (关键：--latest=false)
            if [ -n "$(ls -A download_temp 2>/dev/null)" ]; then
               gh release create "$NEW_TAG" download_temp/* --title "CMFA: $TAG" -F notes.md --latest=false
            else
               gh release create "$NEW_TAG" --title "CMFA: $TAG (无附件)" -F notes.md --latest=false
            fi
            
          done < commoners.txt
          echo "✅ 旧版本全部处理完毕。"

      - name: 3. 独家加冕 (The King)
        env:
          GH_TOKEN: ${{ secrets.MY_PAT }}
          SOURCE_REPO: MetaCubeX/ClashMetaForAndroid
        run: |
          KING_TAG=$(cat king.txt)
          NEW_TAG="CMFA-$KING_TAG"
          
          echo "----------------------------------------"
          echo "👑 正在迎接: $KING_TAG"
          echo "----------------------------------------"
          
          # 准备信息
          gh release view $KING_TAG -R $SOURCE_REPO --json publishedAt,body > meta.json
          ORIG_DATE=$(jq -r .publishedAt meta.json)
          NICE_DATE=$(date -d "$ORIG_DATE" +'%Y-%m-%d %H:%M')
          echo -e "### 📅 原版发布时间: **$NICE_DATE**\n\n---" > notes.md
          jq -r .body meta.json >> notes.md
          
          # 下载
          rm -rf download_temp/*
          gh release download $KING_TAG -R $SOURCE_REPO -D download_temp 2>/dev/null
          
          # 📤 上传并加冕
          # 修复点：create 命令使用 --latest，edit 命令才用 --make-latest
          
          echo "⚡ 执行加冕..."
          
          if gh release view $NEW_TAG > /dev/null 2>&1; then
             echo "⚠️ 版本已存在，执行强制修改..."
             gh release edit "$NEW_TAG" --make-latest
          else
             if [ -n "$(ls -A download_temp 2>/dev/null)" ]; then
                # 这里改成了 --latest (这才是 create 的正确参数)
                gh release create "$NEW_TAG" download_temp/* --title "CMFA: $KING_TAG" -F notes.md --latest
             else
                gh release create "$NEW_TAG" --title "CMFA: $KING_TAG (无附件)" -F notes.md --latest
             fi
          fi
          
          echo "🎉 成功！$KING_TAG 现已是 Latest。"
          rm -rf download_temp raw.txt sorted_tags.txt commoners.txt king.txt meta.json notes.md
