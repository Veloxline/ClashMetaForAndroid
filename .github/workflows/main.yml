name: 2. 纯净搬运 (美学修正版)

on:
  schedule:
    - cron: '0 0 * * 1'
  workflow_dispatch:

jobs:
  aesthetic-sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 初始化
        uses: actions/checkout@v4

      - name: 1. 筛选 >= v2.10.0
        env:
          GH_TOKEN: ${{ secrets.MY_PAT }}
          SOURCE_REPO: MetaCubeX/ClashMetaForAndroid
        run: |
          mkdir -p download_temp
          
          echo "🔍 获取全量列表..."
          gh release list -R $SOURCE_REPO --limit 100 --json tagName -q '.[].tagName' | grep "^v" > raw.txt
          
          # --- Python 筛选 & 排序 ---
          python3 -c "
          import sys
          
          lines = [l.strip() for l in open('raw.txt') if l.strip()]
          filtered = []
          for tag in lines:
              clean_v = tag.lstrip('v').split('-')[0]
              try:
                  parts = [int(x) for x in clean_v.split('.') if x.isdigit()]
                  # 逻辑: >= 2.10.0
                  if len(parts) >= 2:
                      major, minor = parts[0], parts[1]
                      if major > 2 or (major == 2 and minor >= 10):
                          filtered.append(tag)
              except:
                  continue
          
          # 排序: 旧 -> 新
          def sort_key(s):
              nums = s.lstrip('v').split('-')[0].split('.')
              return [int(x) for x in nums if x.isdigit()]
          
          filtered.sort(key=sort_key)
          print('\n'.join(filtered))
          " > tags.txt
          
          # 锁定 King
          KING_TAG=$(tail -n 1 tags.txt)
          echo "KING_TAG=$KING_TAG" >> $GITHUB_ENV

      - name: 2. 执行搬运 (美化处理)
        env:
          GH_TOKEN: ${{ secrets.MY_PAT }}
          SOURCE_REPO: MetaCubeX/ClashMetaForAndroid
        run: |
          set +e
          TOTAL=$(wc -l < tags.txt)
          CURRENT=0
          
          while read TAG; do
            CURRENT=$((CURRENT+1))
            NEW_TAG="CMFA-$TAG"
            
            # 查重
            if gh release view $NEW_TAG > /dev/null 2>&1; then
               echo "⏭️ $NEW_TAG 已存在"
               continue
            fi
            
            echo "🚀 [$CURRENT/$TOTAL] 处理: $TAG"
            
            # 抓取信息
            gh release view $TAG -R $SOURCE_REPO --json publishedAt,body > meta.json
            if [ $? -ne 0 ]; then continue; fi
            
            # --- 🟢 核心美化区 ---
            
            # 1. 时间格式化
            RAW_DATE=$(jq -r .publishedAt meta.json)
            # date 命令: 确保日期和时间中间有空格 (YYYY-MM-DD HH:MM:SS)
            PRETTY_DATE=$(date -d "$RAW_DATE" +'%Y-%m-%d %H:%M:%S')
            
            # 2. 生成说明书 (Note)
            # 去掉了 '### 📅' 这种大标题和图标
            # 改为更精致的加粗文本，不仅好看，而且不占地方
            echo -e "**原版发布时间:** $PRETTY_DATE\n\n---" > notes.md
            jq -r .body meta.json >> notes.md
            
            # 下载
            rm -rf download_temp/*
            gh release download $TAG -R $SOURCE_REPO -D download_temp 2>/dev/null
            
            # 决定是否 Latest
            if [ "$TAG" == "$KING_TAG" ]; then
               LATEST_FLAG="--latest"
            else
               LATEST_FLAG="--latest=false"
            fi
            
            # 3. 标题美化 (去掉 CMFA: 前缀)
            # 使用 "$TAG" 作为标题 (例如 "v2.11.9")
            if [ -n "$(ls -A download_temp 2>/dev/null)" ]; then
               gh release create "$NEW_TAG" download_temp/* --title "$TAG" -F notes.md $LATEST_FLAG
            else
               gh release create "$NEW_TAG" --title "$TAG (无附件)" -F notes.md $LATEST_FLAG
            fi
            
            # 休息 3 秒确保排序
            sleep 3
            
          done < tags.txt
          
          echo "🏁 搬运完成！"
