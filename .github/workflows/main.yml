name: 2. 纯净搬运 (只建不删 | 完美排序)

on:
  schedule:
    - cron: '0 */12 * * *'  # 每天跑两次
  workflow_dispatch:        # 手动触发

jobs:
  pure-sync:
    runs-on: ubuntu-latest
    steps:
      - name: 初始化
        uses: actions/checkout@v4

      - name: 执行排序搬运
        env:
          GH_TOKEN: ${{ secrets.MY_PAT }}
          SOURCE_REPO: MetaCubeX/ClashMetaForAndroid
        run: |
          mkdir -p download_temp
          
          echo "🔍 获取列表并进行数学排序..."
          
          # 1. grep "^v": 只保留 v 开头的正式版 (过滤掉 Prerelease)
          # 2. sort -V: 关键！按版本号从小到大排序 (v2.9 -> v2.10 -> v2.11)
          #    这样脚本会先创建旧版，最后创建新版。
          gh release list -R $SOURCE_REPO --limit 100 --json tagName -q '.[].tagName' \
            | grep "^v" \
            | sort -V > tags.txt
          
          # 检查列表
          if [ ! -s tags.txt ]; then
            echo "❌ 未获取到版本列表，请检查网络或 Token。"
            exit 1
          fi
          
          # 锁定列表中最后一个版本 (也就是版本号最大的)
          LATEST_TARGET=$(tail -n 1 tags.txt)
          echo "🎯 本次锁定的最新版本是: $LATEST_TARGET"
          
          TOTAL=$(wc -l < tags.txt)
          CURRENT=0
          
          while read TAG; do
            CURRENT=$((CURRENT+1))
            NEW_TAG="CMFA-$TAG"
            
            # --- 查重 (遇到已存在的直接跳过，不碰它) ---
            if gh release view $NEW_TAG > /dev/null 2>&1; then
              echo "[$CURRENT/$TOTAL] ✅ $NEW_TAG 已存在，跳过。"
              continue
            fi
            
            echo "----------------------------------------"
            echo "🚀 [$CURRENT/$TOTAL] 正在搬运: $TAG"
            
            # 1. 抓取时间 (仅做展示)
            RAW_DATE=$(gh release view $TAG -R $SOURCE_REPO --json publishedAt -q .publishedAt 2>/dev/null) || true
            if [ -n "$RAW_DATE" ]; then
                NICE_DATE=$(date -d "$RAW_DATE" +'%Y-%m-%d %H:%M')
            else
                NICE_DATE="Unknown"
            fi
            
            # 2. 准备日志
            gh release view $TAG -R $SOURCE_REPO --json body -q .body > logs.txt
            echo -e "### 📅 原版发布时间: **$NICE_DATE**\n\n---" > notes.md
            cat logs.txt >> notes.md
            
            # 3. 下载
            rm -rf download_temp/*
            gh release download $TAG -R $SOURCE_REPO -D download_temp 2>/dev/null || true
            
            # 4. 上传 (不指定 latest，依靠时间戳排序)
            if [ "$(ls -A download_temp)" ]; then
               gh release create $NEW_TAG download_temp/* --title "CMFA: $TAG" -F notes.md --latest=false
            else
               gh release create $NEW_TAG --title "CMFA: $TAG (无附件)" -F notes.md --latest=false
            fi
            
            echo "✅ $NEW_TAG 完成"
            
            # 🟢 关键：强制睡眠 3 秒
            # 确保下一个版本的创建时间比这个晚
            sleep 3
            
          done < tags.txt
          
          # --- 终极修正：给最新版加冕 ---
          if [ ! -z "$LATEST_TARGET" ]; then
            FINAL_TAG="CMFA-$LATEST_TARGET"
            echo "👑 正在将 $FINAL_TAG 标记为 Latest..."
            gh release edit "$FINAL_TAG" --make-latest || echo "⚠️ 标记失败，可能是该版本未被创建"
          fi
          
          rm -rf download_temp tags.txt logs.txt notes.md
          echo "🏁 所有任务完成！"
