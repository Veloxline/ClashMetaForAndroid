name: 2. 纯净搬运 (ID修正时光机)

on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  time-travel-id-fix:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 必须有写入权限
    steps:
      - name: 初始化
        uses: actions/checkout@v4

      - name: 执行搬运并篡改时间
        env:
          GH_TOKEN: ${{ secrets.MY_PAT }}
          SOURCE_REPO: MetaCubeX/ClashMetaForAndroid
        run: |
          # 1. 获取当前仓库的 owner/repo 格式
          MY_REPO=$(gh repo view --json owner,name -q ".owner.login + \"/\" + .name")
          echo "📍 锁定当前仓库: $MY_REPO"
          
          mkdir -p download_temp
          
          echo "🔍 获取原始列表..."
          # 获取列表，只保留 v 开头
          gh release list -R $SOURCE_REPO --limit 100 --json tagName -q '.[].tagName' | grep "^v" > tags.txt
          
          TOTAL=$(wc -l < tags.txt)
          CURRENT=0
          
          while read TAG; do
            CURRENT=$((CURRENT+1))
            NEW_TAG="CMFA-$TAG"
            
            # --- 查重 ---
            if gh release view $NEW_TAG > /dev/null 2>&1; then
              echo "[$CURRENT/$TOTAL] ✅ $NEW_TAG 已存在，跳过。"
              continue
            fi
            
            echo "----------------------------------------"
            echo "🚀 [$CURRENT/$TOTAL] 正在搬运: $TAG"
            
            # 1. 抓取原版精确时间
            gh release view $TAG -R $SOURCE_REPO --json publishedAt,body > meta.json
            ORIG_DATE=$(jq -r .publishedAt meta.json)
            NICE_DATE=$(date -d "$ORIG_DATE" +'%Y-%m-%d %H:%M')
            
            echo -e "### 📅 原版发布时间: **$NICE_DATE**\n\n---" > notes.md
            jq -r .body meta.json >> notes.md
            
            # 2. 下载
            rm -rf download_temp/*
            gh release download $TAG -R $SOURCE_REPO -D download_temp 2>/dev/null || true
            
            # 3. 创建发布 (此时时间是“现在”)
            if [ "$(ls -A download_temp)" ]; then
               gh release create $NEW_TAG download_temp/* --title "CMFA: $TAG" -F notes.md --latest=false
            else
               gh release create $NEW_TAG --title "CMFA: $TAG (无附件)" -F notes.md --latest=false
            fi
            
            # 🟢 4. 核心修正：获取 Release ID 并通过 ID 修改时间
            # 这是解决 404 错误的唯一办法
            echo "🔍 正在查询 $NEW_TAG 的唯一 ID..."
            RELEASE_ID=$(gh release view "$NEW_TAG" --json databaseId -q .databaseId)
            
            echo "🔧 正在将 ID:$RELEASE_ID 的时间修正为: $ORIG_DATE"
            gh api --method PATCH "/repos/$MY_REPO/releases/$RELEASE_ID" -f published_at="$ORIG_DATE"
            
            echo "✅ $NEW_TAG 完成 (时间已穿越)"
            
          done < tags.txt
          
          # --- 5. 自动加冕最新版 ---
          # 由于我们恢复了真实时间，GitHub 应该能自动识别，但为了保险还是手动标记一下
          echo "👑 正在刷新 Latest 状态..."
          LATEST_TAG=$(gh release list --limit 1 --exclude-drafts --exclude-pre-releases --json tagName -q '.[0].tagName')
          if [ ! -z "$LATEST_TAG" ]; then
             echo "🎯 检测到最新版是 $LATEST_TAG，正在加冕..."
             gh release edit "$LATEST_TAG" --make-latest || true
          fi
          
          rm -rf download_temp tags.txt meta.json notes.md
          echo "🏁 完美收官！"
